#include <stdio.h>
/**
 *  main - using the printf function to print the given strings
 *
 * Return: Always 0 on success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
