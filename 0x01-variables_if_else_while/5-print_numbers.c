#include <stdio.h>
/**
 * main - print numbers in base 10
 * Return: return 0 if successful
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%i", num);
	}
	printf("\n");
	return (0);
}
