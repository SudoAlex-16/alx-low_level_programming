#include <stdio.h>
/**
 * main - prints alphabets in lowercase
 * Return: return 0 if successful
 */

int main(void)
{
	char cha;

	for (cha = 'a'; cha <= 'z' ; cha++)
	{
		putchar(cha);
	}
		putchar('\n');
	return (0);
}
