#include "main.h"
#include <stdio.h>
/**
 *  main - prints the absolute value
 *  Return: return 0 if successful
 */

int _abs(int)
{
	int a;

	if (a < 0)
	{
		return (a * -1);
	}
	else
	{
		return (a);
	}
}
