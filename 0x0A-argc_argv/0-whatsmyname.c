#include <stdio.h>
/**
 * main - Entry point of the program with the two arguments
 * @argc: This is the vector count which will be unused
 * @argv: This is the one dimentional array which will print
 * the program name.
 * Return: Return 0 if successful
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
